<% if !current_user %>
  <%= render 'layouts/logged_out' %>
<% else %>
  <%= render 'layouts/navbar_profile' %>
  <%= render 'layouts/navbar_secondary' %>

  <div class="container">
    <div class="row text-center mt-2 mb-2">
      <div class="col-12">
        <% if @user.avatar.attachment.nil? %>
          <%= image_tag('user.svg', class:'btn-block')%>
        <% else %>
          <%= image_tag(@user.avatar.service_url, class:'btn-block')%>
        <% end %>
      </div>
    </div>

    <div class="row text-center mt-1 mb-1">
      <div class="col-1"></div>
      <div class="col-1"></div>
      <div class="col-8">swipefeedback</div>
      <div class="col-1">
        <% if current_user == @user %>
          <%= link_to image_tag('edit.png', height: 11, width: 11, class:''), user_edit_bio_url(@user) %>
        <% end %>
      </div>
      <div class="col-1"></div>
    </div>

    <div class="row mb-3">
      <div class="col-12">About Me</div>
    </div>

    <div class="row text-center">
      <div class="col-12"><%= @user.bio %></div>
    </div>

    <% if !@practices.empty? || !@specialties.empty? %>
      <div class="color-bottom-border mt-3 mb-3"></div>
    <% end %>

    <% if !@practices.empty? %>
      <div class="row text-center mb-3">
        <div class="col-12"><h6>I teach</h6></div>
      </div>
      <div class="row text-center">
        <div class="col-12">
          <% @practices.each do |practice| %>
            <p><%= practice.practice_category.name %> </p>
          <% end %>
        </div>
      </div>
    <% end %>

    <% if !@specialties.empty? %>
      <div class="row text-center mb-3">
        <div class="col-12"><h6>I focus on</h6></div>
      </div>
      <div class="row text-center">
        <div class="col-12">
          <% @specialties.each do |specialty| %>
            <p><%= specialty.specialty_category.name %> </p>
          <% end %>
        </div>
      </div>
    <% end %>

    <% if !@milestones.empty? %>
      <div class="color-bottom-border mt-3 mb-3"></div>

      <div class="row text-center">
        <div class="col-12"><h6>Certifications & Experience</h6></div>
      </div>
      <div class="row text-center mb-5">
        <div class="col-12">
          <% @milestones.each do |milestone| %>
            <div class="row text-center">
              <div class="col-2"></div>
              <div class="col-2"><p><%= milestone.year %></p></div>
              <div class="col-1"></div>
              <div class="col-6"><p><%= milestone.milestone %></p></div>
              <div class="col-1"></div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>

  <% if current_user.role == 0 %>
    <%= render 'layouts/footer_trainee' %>
  <% elsif current_user.role == 1 %>
    <%= render 'layouts/footer_trainer' %>
  <% end %>
<% end %>
